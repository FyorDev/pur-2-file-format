PureRef 2.0.3 (Linux): enums, methods and properties from its meta objects
Generated by: python -m investigation.qmetaobject <unpacked PureRef binary>
  156 meta objects, 15 enums, 57 keys, 249 methods, 191 properties

  Action   (moc revision 10)
    signal void metadataChanged()
    signal void bindingsChanged(Action* action, QList<ActionBinding> oldBindings)
    signal void continuousBegin(QPointF pos, Action* transitioningFrom)
    signal void continuousBegin(QPointF pos)
    signal void continuousUpdate(Action::ContinuousUpdateData pos)
    signal void continuousEnd(Action::ContinuousUpdateData pos, Action* transitioningTo)
    signal void continuousEnd(Action::ContinuousUpdateData pos)

  ActionHandler   (moc revision 10)
    signal void actionCreated(Action* newAction)
    signal void actionRemoved(Action* newAction)
    signal void globalHandlerErrorEncountered(Error error)

  ActionState   (moc revision 10)
    signal void stateChanged()
    signal void blockedAction(QList<ActionState*> disabledReasons)

  AlignCommand : TransformCommand   (moc revision 10)
    enum scoped ItemAlign
                0 = Left
                1 = Right
                2 = Top
                3 = Bottom
                4 = DistributeVertical
                5 = DistributeHorizontal
                6 = Stack
                7 = SortName
                8 = SortPath
                9 = SortOrder
               10 = SortAddition
               11 = Optimal

  AlphaSlider   (moc revision 10)
    property int borderRadius
    property int indicatorRadius
    signal void colorChanged(type#4099 newColor)
    signal void colorCommitted(type#4099 newColor)

  AnalyticsHandler   (moc revision 10)
    signal void configFetched()

  Application   (moc revision 10)
    enum scoped OpenInMode
                0 = NewWindow
                1 = CurrentWindow
                2 = NewOrExisting

  AutoSaveRecoveryDialog   (moc revision 10)
    property type#4099 buttonColor
    property type#4099 buttonHoverColor
    property type#4099 buttonHoverBackgroundColor
    property type#4099 hoverColor
    property type#4099 textHoverColor
    property type#4099 textSelectedColor
    property int buttonBorderRadius
    property QSize buttonPadding
    property int rowPadding
    property int rowMinHeight
    property int columnSpacing
    property int iconPadding
    property type#4099 spinnerColor
    property type#4099 spinnerBackgroundColor
    property type#4099 warningColor
    property type#4099 errorColor
    signal void recoverRequested(AutoSaveRestoreEntry* restoreEntry)
    signal void recoverInstantRequested(AutoSaveRestoreEntry* restoreEntry)

  AutoUpdater   (moc revision 10)
    property int progressBarHeight
    signal void versionCheckStarted()
    signal void versionCheckFinished(VersionCheckResult result, QString message)
    signal void updateAvailable(UpdateMessage message)
    signal void finishedUpdating(QString sceneToReopen, QString newApplicationPath, bool forcedUpdate)
    signal void updateRequested()
    signal void aboutToRestart()

  BaseCommand   (moc revision 10)
    signal void commandMessage(QString message)

  BaseFrame   (moc revision 10)
    property int contentPadding
    signal void dropRequested(const QMimeData* data)

  BaseLoader   (moc revision 10)
    signal void started(QString operationName, LoadJob* loadJob)
    signal void progressRangeChanged(int min, int max, LoadJob* loadJob)
    signal void progressChanged(int value, LoadJob* loadJob)
    signal void progressMessage(QString message, bool elipsis, LoadJob* loadJob)
    signal void itemGenerated(GraphicsItem* item, LoadJob* loadJob)
    signal void finished(LoadJob* loadJob)

  CheckBox   (moc revision 10)
    property type#4099 hoverColor
    property type#4099 backgroundHoverColor
    property type#4099 borderColor
    property int borderRadius
    property int padding

  ColorButton   (moc revision 10)
    property int radius

  ColorCustomizer : CustomizeSettings   (moc revision 10)
    signal void styleUpdated(QString updatedStyle)
    signal void fontUpdated(type#4096 updatedFont)
    signal void masterOpacityUpdated(double updatedMasterOpacity)

  ColorPickerButton : IconButton   (moc revision 10)
    signal void colorPicked(type#4099 color)
    signal void colorCommitted(type#4099 newColor)

  ColorPreset   (moc revision 10)
    signal void updated()

  ColorSelector   (moc revision 10)
    signal void finishedSelectingColor(type#4099 color)
    signal void canceledSelectingColor()
    signal void selectedColorChanged(type#4099 color)
    signal void colorCommitted()

  ColorSelectorCommandManager   (moc revision 10)
    signal void colorUpdated(type#4099 color)
    signal void colorCommitted(type#4099 color)
    signal void selectorClosed()

  ColorValuesWidget   (moc revision 10)
    signal void colorChanged(type#4099 newColor)
    enum scoped ColorType
                0 = HEX
                1 = RGB
                2 = HSB
                3 = LAB
                4 = CSS

  CommandHandler   (moc revision 10)
    signal void changed()
    signal void aboutToExecuteCommands()
    signal void commandsExecuted(QList<BaseCommand*> commands, ExecuteType type)
    signal void commandMessage(QString message)

  CommandPalette : ToolWindow   (moc revision 10)
    property type#4099 buttonColor
    property type#4099 buttonHoverColor
    property type#4099 buttonHoverBackgroundColor
    property type#4099 hoverColor
    property type#4099 textHoverColor
    property type#4099 textSelectedColor
    property int buttonBorderRadius
    property QSize buttonPadding
    property int rowPadding
    property int rowMinHeight
    property int columnSpacing
    property int iconPadding
    property type#4099 disabledColor

  CommandSaver   (moc revision 10)
    signal void saveFailed(Error error)

  ConfirmDialog   (moc revision 10)
    property int labelMaxWidth

  ContextMenu   (moc revision 10)
    property type#4102 checkIcon
    property type#4099 hoverColor
    property type#4099 hoverBackgroundColor
    property int itemHMargin
    property int itemCheckExtraSpace
    property int itemCheckPadding
    property int itemVMargin
    property int separatorHMargin
    property int separatorHeight
    property int borderRadius

  CoordinatePickerDialog : ToolWindow   (moc revision 10)
    signal void hidden()

  CopyPasteHandler : BaseLoader   (moc revision 10)
    signal void itemsCopyRequested(QList<GraphicsItem*> items)

  CustomizeSettings   (moc revision 10)
    property int maxLabelWidth
    property int maxSettingWidth

  DialogButton   (moc revision 10)
    property int iconPadding
    property type#4099 iconHoverColor
    property int padding

  DrawToolbar : Toolbar   (moc revision 10)
    signal void toggleDraw()

  EmptySceneWidget   (moc revision 10)
    signal void browseRequested()
    signal void helpRequested()

  FontComboBox   (moc revision 10)
    signal void currentFontChanged(type#4096 font)

  Frame : BaseFrame   (moc revision 10)
    signal void contextMenuRequested()

  GlobalKeyEventHandler   (moc revision 10)
    signal void keysPressed(int nativeKey, Qt::Key key, Qt::KeyboardModifiers modifiers, Qt::MouseButtons buttons)
    signal void startListening()

  GraphicsGroupItem : GraphicsItem   (moc revision 10)
    enum scoped LockMode
                0 = Open
                1 = Closed

  GraphicsImageItem : GraphicsItem   (moc revision 10)
    signal void imageLoadFinished()
    signal void playbackStateChanged(PlaybackState newState)
    signal void playbackSpeedChanged(float newSpeed)
    signal void currentFrameChanged(int frame)

  GraphicsItemModel   (moc revision 10)
    signal void nameChangeRequested(GraphicsItem* item, QString newName)
    signal void moveRequested(QList<GraphicsItem*> items, GraphicsItem* parent, BigRational moveToBegin, BigRational moveToEnd)
    signal void copyRequested(QList<GraphicsItem*> items, GraphicsItem* parent, BigRational copyToBegin, BigRational copyToEnd)

  GraphicsItemTreeView : TreeView   (moc revision 10)
    property type#4099 buttonColor
    property type#4099 buttonHoverColor
    property type#4099 buttonHoverBackgroundColor
    property type#4099 hoverColor
    property type#4099 textHoverColor
    property type#4099 textSelectedColor
    property int buttonBorderRadius
    property QSize buttonPadding
    property int rowPadding
    property int rowMinHeight
    property int columnSpacing
    property int iconPadding
    signal void copyRequested()
    signal void pasteRequested()
    signal void positionedPasteRequested(GraphicsItem* parentItem, BigRational pasteToBegin, BigRational pasteToEnd)

  GraphicsScene   (moc revision 10)
    signal void aboutToClear()
    signal void clearedChanged(bool isCleared)
    signal void itemCountChanged(int count)
    signal void itemAboutToBeAdded(int newIndex, GraphicsItem* parent)
    signal void itemAdded(GraphicsItem* item)
    signal void itemAboutToBeRemoved(GraphicsItem* item)
    signal void itemRemoved(GraphicsItem* item)
    signal void itemAboutToMove(GraphicsItem* item, int newIndex, GraphicsItem* newParent)
    signal void itemMoved(GraphicsItem* item, int oldIndex, GraphicsItem* oldParent)
    signal void itemSelected(GraphicsItem* item, bool selected)

  GraphicsTextItem : GraphicsItem   (moc revision 10)
    signal void textChanged()
    signal void contentsChanged()
    signal void currentCharFormatChanged()
    signal void cursorPositionChanged()
    signal void selectionChanged()
    signal void undoCommandAdded()
    signal void inputAboutToBeHandled()
    signal void fixedSizeChanged()
    signal void geometryChanged()

  GraphicsView   (moc revision 10)
    property type#4099 canvasColor
    property type#4099 itemSelectionColor
    property type#4099 itemBorderColor
    property type#4099 itemBackgroundColor
    property type#4099 itemPlaceholderColor
    property type#4099 itemPlaceholderTextColor
    signal void emptyCanvasDoubleClicked()
    signal void updateStarted()
    signal void updateFinished()
    signal void focusedMissingImage(GraphicsImageItem* image)
    signal void dropRequested(const QMimeData* mimeData, QPointF pos)
    signal void visibleSceneRectChanged()
    signal void visibleSceneRectChangedInstant()
    signal void requestUndo()
    signal void requestRedo()
    signal void unpickableItemClicked(GraphicsItem* item)
    signal void purLinkClicked(QString link, bool newWindow)

  GroupToolbar : Toolbar   (moc revision 10)
    property type#4099 groupBackgroundColor

  HtmlBrowser   (moc revision 10)
    property QString style

  HtmlDisplayDialog   (moc revision 10)
    signal void linkClicked(QUrl link)

  HueSlider   (moc revision 10)
    property int borderRadius
    property int indicatorRadius
    signal void colorChanged(type#4099 newColor)
    signal void colorCommitted(type#4099 newColor)

  IconButton   (moc revision 10)
    property type#4099 hoverColor
    property int padding

  ImageCache   (moc revision 10)
    signal void errorEncountered(Error error)
    signal void cacheModeChanged(CacheMode newCacheMode)
    signal void maxMipCountChanged(int mipCount)
    enum scoped CacheMode
                0 = NoCache
                1 = OnDisk
                2 = InMemory

  ImageExporter : BaseLoader   (moc revision 10)
    signal void imageRenderRequested(int imageIndex)

  ImageLoader : BaseLoader   (moc revision 10)
    signal void purFileFound(QString path)
    signal void validatePasteText(QString text, LoadJob* loadJob)
    signal void textItemSetupRequested(GraphicsTextItem* item, QString text, bool html)
    signal void doneFetching()

  ItemViewDelegate   (moc revision 10)
    property type#4099 buttonColor
    property type#4099 buttonHoverColor
    property type#4099 buttonHoverBackgroundColor
    property type#4099 hoverColor
    property type#4099 textHoverColor
    property type#4099 textSelectedColor
    property int buttonBorderRadius
    property QSize buttonPadding
    property int rowPadding
    property int rowMinHeight
    property int columnSpacing
    property int iconPadding
    signal void buttonPressed(QModelIndex index)

  KeyBinder   (moc revision 10)
    property type#4099 buttonColor
    property type#4099 buttonHoverColor
    property type#4099 buttonHoverBackgroundColor
    property type#4099 hoverColor
    property type#4099 textHoverColor
    property type#4099 textSelectedColor
    property int buttonBorderRadius
    property QSize buttonPadding
    property int rowPadding
    property int rowMinHeight
    property int columnSpacing
    property int iconPadding

  KeyEdit : LineEdit   (moc revision 10)
    signal void keySequenceChanged(QKeySequence newSequence)

  KeyListener   (moc revision 10)
    signal void keysPressed(int nativeKey, Qt::Key key, Qt::KeyboardModifiers modifiers, Qt::MouseButtons buttons)
    signal void listening()
    slot void startListening()
    slot void emitBindingPress()

  LineEdit   (moc revision 10)
    property int clearButtonOverlap
    property type#4099 placeholderColor

  LoadJob   (moc revision 10)
    signal void started(QString operationName)
    signal void progressRangeChanged(int min, int max)
    signal void progressChanged(int value)
    signal void itemGenerated(GraphicsItem* item)
    signal void finished()
    signal void canceledCalled()

  LoaderCircle   (moc revision 10)
    property double timerProgress
    property type#4099 color
    property type#4099 outlineColor
    property double thickness
    property double outlineThickness

  LocalCommandHandler   (moc revision 10)
    signal void done()
    signal void writeDone()
    signal void readDone(QByteArray readData)
    signal void writeRequested(QByteArray writeData)
    signal void cancelRequested()
    signal void finishExpected()

  LogDialog   (moc revision 10)
    property QString style

  LoggingHandler   (moc revision 10)
    signal void messageLogged(LogMessage message)

  MainWindow   (moc revision 10)
    signal void loadRequested()
    signal void cancelAllLoadJobs()
    signal void loaderDone()
    slot void setMouseTransparent(bool transparent, bool force)
    slot void setMouseTransparent(bool transparent)
    slot void toggleMouseTransparent()
    slot void setOverlay(bool overlay, bool force)
    slot void setOverlay(bool overlay)
    slot void toggleOverlay()
    slot void setWindowLocked(bool locked)
    slot void startNewInstance(QString applicationPath, QStringList extraArgs, bool deElevate)
    slot void resizeToSelection()
    slot void clearScene()
    slot void forceClear()
    slot void loadMostRecentFile()
    slot void loadRecentFile(int recentIndex)
    slot void loadImages()
    slot void loadPhotos()
    slot void loadCameraPhotos()
    slot void loadImage(LoadSource& loadSource)
    slot void loadImageFromClipboard(QPointF pos)
    slot void loadImageFromClipboard()
    slot void handleOpenFileRequest(QList<QUrl> files)
    slot void updateNumOfRecentLoadedFiles()
    slot bool tryClose()
    slot void handleUpdateRequested()
    slot void restart(QString sceneToReopen, QString newApplicationPath, bool deElevate)
    slot void undo()
    slot void redo()
    slot void showMainMenu()
    slot void showHelp()
    slot void showAbout()
    slot void showKeybinder()
    slot void showSettings()
    slot void setCanvasLocked(bool locked)
    slot void increaseMasterOpacity()
    slot void decreaseMasterOpacity()
    slot void setWindowMode(WindowMode mode)
    slot void hardExit(int exitCode)
    enum scoped PerformanceMode
                0 = NoCache
                1 = OnDisk
                2 = InMemory
                3 = InMemoryNoMips
    enum scoped CopyBackgroundMode
                0 = NoBackground
                1 = AlwaysBackground
                2 = BackgroundIfMultiselect

  MouseButtonEdit : LineEdit   (moc revision 10)
    signal void buttonsChanged(Qt::MouseButtons newButtons)

  Movie   (moc revision 10)
    signal void stateChanged(State state)
    signal void frameChanged(int frameNumber)
    signal void playbackSpeedChanged(float speed)
    enum scoped State
                0 = NotRunning
                1 = Paused
                2 = Running

  NormalizeScale   (moc revision 10)
    enum scoped ResizeRule
                0 = SameWidth
                1 = SameHeight
                2 = SameSize
                3 = SameScale
    enum scoped SizeSource
                0 = Average
                1 = First

  NoteEditingToolbar : NoteToolbar   (moc revision 10)
    property QString defaultTextStyle

  NoteToolbar : Toolbar   (moc revision 10)
    property type#4099 linkColor
    property type#4099 noteTextColor
    property type#4099 noteBackgroundColor

  Notification : OverlayPanel   (moc revision 10)
    property int margin
    property int padding
    property int spacing

  OverlayHandler   (moc revision 10)
    signal void foundOverlayWindow(QString windowTitle, QRect geometry)
    signal void windowGeometryChanged(QRect geometry, UpdateOffsetMode updateOffset)
    signal void windowFocusChanged(bool focused)
    signal void windowMinimized()
    signal void windowDestroyed()
    signal void errorEncountered(Error error)
    signal void startListening()

  OverlayLayoutProvider   (moc revision 10)
    signal void availableRectChanged()

  OverlayPanel : OverlayWidget   (moc revision 10)
    property int dropShadow
    property QPoint dropShadowOffset
    property type#4099 dropShadowColor
    property int fadeDuration

  PlatformHandler   (moc revision 10)
    signal void runningOutOfMemory()
    signal void loadRequested(QString filename)
    signal void newSaveRequested(QString fileName)
    signal void dropRequested(LoadSource& data)

  PlaybackToolbar : Toolbar   (moc revision 10)
    property type#4099 frameHoverColor
    property type#4099 frameSelectedColor
    property int framePadding
    property int frameCornerRadius
    signal void playbackUpdated(QList<GraphicsImageItem*> changedItems)

  PopupLabel : OverlayPanel   (moc revision 10)
    property int iconPadding
    property int iconSize
    property int labelHMargin
    property int labelPadding
    property int labelSpacing
    property int borderRadius
    property type#4099 backgroundColor
    property type#4099 borderColor

  PopupMenu : PopupWidget   (moc revision 10)
    property type#4099 hoverColor
    property int itemHMargin
    property int itemVMargin
    property int iconWidth
    property int iconMargin

  PopupWidget : OverlayPanel   (moc revision 10)
    property int pushMargin
    property int borderRadius
    property int arrowWidth
    property int arrowHeight

  ProgressBar   (moc revision 10)
    property type#4099 foregroundColor
    property type#4099 backgroundColor
    property float progressAnimationPos

  ProgressEntry   (moc revision 10)
    signal void valueChanged(int value)
    signal void rangeChanged(int min, int max)
    signal void infoTextChanged(QString text, int numLoadingDots)
    signal void canceled()

  ProgressSlider   (moc revision 10)
    signal void valueChanged(int value)
    signal void seekingStarted()
    signal void seekingEnded()
    signal void seekValueChanged(int value)

  ProgressWidget : OverlayPanel   (moc revision 10)
    property float yRelPos
    property int maxWidth

  RelinkDialog   (moc revision 10)
    property type#4099 buttonColor
    property type#4099 buttonHoverColor
    property type#4099 buttonHoverBackgroundColor
    property type#4099 hoverColor
    property type#4099 textHoverColor
    property type#4099 textSelectedColor
    property int buttonBorderRadius
    property QSize buttonPadding
    property int rowPadding
    property int rowMinHeight
    property int columnSpacing
    property int iconPadding
    property type#4099 failColor
    property type#4099 notFoundColor
    property type#4099 relinkedColor
    signal void relinkRequested(MissingImage items)
    signal void massRelinkRequested(RelinkGroup* group)
    signal void relinkingDone()

  Relinker : BaseLoader   (moc revision 10)
    signal void itemRelinked(Relinker::ItemData item, ResourceHandle imageHandle, LoadJob* loadJob)

  ResourceLoadRequest   (moc revision 10)
    signal void finished(ResourceHandle loadedHandle)
    signal void releasing()

  RichTextLabel   (moc revision 10)
    property QString style

  SaturationBrightnessPicker   (moc revision 10)
    property int indicatorRadius
    signal void colorChanged(type#4099 newColor)
    signal void colorCommitted(type#4099 newColor)

  SaveFileLoader : BaseLoader   (moc revision 10)
    signal void checksumLoaded(QString loadedChecksum, QString actualChecksum, LoadJob* loadJob)
    signal void checksumLoadedBeforeSave(QString loadedChecksum, QString actualChecksum, LoadJob* loadJob)
    signal void checksumSaved(QString checksum, LoadJob* loadJob)
    signal void checksumMismatchDetected(LoadJob* loadJob, bool sqlite)
    signal void brokenItemMetadataDetected(LoadJob* loadJob, bool sqlite)
    signal void loadedSceneState(SceneStateData sceneState, LoadJob* loadJob)
    signal void loadWarning(QString warning)
    signal void itemImageLoaded(GraphicsImageItem* item, ResourceHandle image, LoadJob* loadJob)
    signal void aboutToOverwriteSaveFromNewerVersion(QString applicationVersion, LoadJob* loadJob)
    signal void aboutToOverwriteOldFormat(LoadJob* loadJob)
    signal void aboutToOverwriteBrokenFile(LoadJob* loadJob)
    signal void sceneClearRequested(bool clearSceneSerializer)
    signal void sceneClearRequested()
    signal void partialItemGenerated(GraphicsItem* item, LoadJob* loadJob)

  SaveLoadHandler   (moc revision 10)
    signal void savePathChanged(QString path)
    signal void saveStarted(QString path)
    signal void saveFinished(QString path, qlonglong duration, bool success)
    signal void saveFailed()
    signal void saveFailureResolved()
    signal void loadStarted(QString path)
    signal void loadFinished(QString path, qlonglong duration, bool success, bool clear)
    enum scoped UnsavedSceneBehavior
                0 = Prompt
                1 = Discard
                2 = Save

  SceneSerializerSqlite   (moc revision 10)
    signal void syncFinished()
    signal void syncFailed(Error error)

  SceneStatusIcon : OverlayWidget   (moc revision 10)
    property double maxOpacity
    property int iconSize
    property int iconPadding
    property int padding

  Setting   (moc revision 10)
    signal void changed(QVariant oldValue, QVariant newValue)

  ShadowAndOpacityEffect   (moc revision 10)
    property double opacity
    property QPointF offset
    property double xOffset
    property double yOffset
    property double blurRadius
    property type#4099 color
    signal void opacityChanged(double opacity)
    signal void offsetChanged(QPointF offset)
    signal void blurRadiusChanged(double blurRadius)
    signal void colorChanged(type#4099 color)

  SlideshowControls : Toolbar   (moc revision 10)
    property int circlePadding
    signal void imageRequested(int index)
    signal void started()
    signal void closeRequested()
    enum scoped FadeMode
                0 = Controls
                1 = All
                2 = None
    enum scoped StepMode
                0 = Ordered
                1 = Shuffle
                2 = Random

  SocketMonitor   (moc revision 10)
    signal void disconnected()
    signal void finishedReading(QByteArray readData, bool success)
    signal void finishedWriting()

  SpinBox   (moc revision 10)
    signal void valueChanged(double newValue)
    signal void valueScrolled(double newValue)
    signal void editingFinished()

  TimedLineEdit : LineEdit   (moc revision 10)
    signal void textEditingTimedOut(QString newText)

  TitleBarHandler   (moc revision 10)
    enum scoped FadeMode
                0 = Unset
                1 = Always
                2 = MouseOver
                3 = MouseMove
                4 = Never

  ToastHandler   (moc revision 10)
    signal void errorQueued(Error error)

  Toolbar : OverlayPanel   (moc revision 10)
    property int xOffset
    property int yOffset
    property int buttonsMargin
    property int buttonsSpacing
    property int parentPadding
    enum scoped FadeMode
                0 = None
                1 = OnIdle

  ToolbarButton : IconButton   (moc revision 10)
    property int dropdownWidth
    property int dropdownPadding

  TransformGizmo   (moc revision 10)
    signal void settingsUpdated()

  TreeView   (moc revision 10)
    property int indent
    property int expanderIconPadding
    property type#4099 branchColor
    signal void itemHovered(QModelIndex index)

  TruncatedLabel   (moc revision 10)
    property int maxWidth

  UpdateIcon   (moc revision 10)
    property type#4099 failColor
    property type#4099 unknownColor
    property type#4099 successColor

  WidgetFader   (moc revision 10)
    signal void aboutToShow()
    signal void finishedFading(FadeDirection direction)

  WidgetFaderDelegate   (moc revision 10)
    property double opacity

  WindowListener   (moc revision 10)
    signal void foundOverlayWindow(QString windowTitle, QRect geometry)
    signal void windowGeometryChanged(QRect geometry)
    signal void windowFocusChanged(bool focused)
    signal void windowMinimized()
    signal void windowDestroyed()
